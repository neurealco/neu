name: Production Deployment

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup SSH environment
        uses: webfactory/ssh-agent@v0.8.0
        with:
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: Configure known hosts
        run: |
          mkdir -p ~/.ssh
          ssh-keyscan -t rsa,ed25519,ecdsa ${{ secrets.SSH_HOST }} >> ~/.ssh/known_hosts

      - name: Verify SSH connection
        run: |
          ssh -o StrictHostKeyChecking=accept-new \
              -o ConnectTimeout=10 \
              ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }} \
              "echo '✅ SSH connection successful!'"

      - name: Ensure .env exists
        run: |
          ssh -o StrictHostKeyChecking=accept-new \
              ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }} << 'ENV_EOF'
          set -e
          cd /opt/neureal/apps/backend

          if [ ! -f .env ]; then
            echo "⚠️ .env file missing, creating new one..."
            touch .env
            echo "# Auto-generated .env file" >> .env
            echo "APP_ENV=production" >> .env
            if [ -f .env.example ]; then
              echo "🔧 Populating from .env.example"
              cat .env.example >> .env
            fi
            echo "LOG_LEVEL=info" >> .env
          else
            echo "🔑 .env file already exists"
          fi
          ENV_EOF

      - name: Pre-deploy setup
        run: |
          ssh -o StrictHostKeyChecking=accept-new \
              ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }} << 'PRE_EOF'
          set -e
          cd /opt/neureal
          
          # Detener y deshabilitar servicios conflictivos
          sudo systemctl stop nginx apache2 || true
          sudo systemctl disable nginx apache2 || true
          sudo pkill -9 nginx || true
          
          # Verificar si se necesitan certificados SSL
          export SSL_CERTS_EXIST=$(test -d "certbot/conf/live/neureal.site" && echo "1" || echo "0")
          PRE_EOF

      - name: Deploy application
        run: |
          ssh -o StrictHostKeyChecking=accept-new \
              -o ConnectTimeout=20 \
              ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }} << 'EOF'
          set -e
          set -x

          cd /opt/neureal

          # Guardar el estado actual de .env antes de actualizar
          if [ -f apps/backend/.env ]; then
            echo "💾 Backing up existing .env"
            cp apps/backend/.env apps/backend/.env.tmp
          fi

          # Actualizar código
          git fetch origin
          git reset --hard origin/main

          # Restaurar .env si existía
          if [ -f apps/backend/.env.tmp ]; then
            echo "🔄 Restoring .env from temporary backup"
            mv apps/backend/.env.tmp apps/backend/.env
          fi

          # Crear .env si no existe después de actualizar
          if [ ! -f apps/backend/.env ]; then
            echo "⚠️ .env still missing, creating minimal version"
            touch apps/backend/.env
            echo "APP_ENV=production" >> apps/backend/.env
            echo "LOG_LEVEL=info" >> apps/backend/.env
          fi

          # Limpiar contenedores antiguos
          docker-compose down --remove-orphans || true

          # Construir imágenes
          docker-compose build --no-cache --pull backend frontend nginx

          # Iniciar todos los servicios EXCEPTO certbot
          docker-compose up -d frontend backend redis nginx

          echo "⏳ Waiting for services to be ready..."
          sleep 10
          EOF

      - name: Handle SSL certificates
        run: |
          ssh -o StrictHostKeyChecking=accept-new \
              ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }} << 'SSL_EOF'
          set -e
          cd /opt/neureal
          
          # Solo configurar SSL si no existe
          if [ "$SSL_CERTS_EXIST" = "0" ]; then
            echo "🔐 Initial SSL certificate setup"
            
            # Crear directorios necesarios
            mkdir -p certbot/conf certbot/www
            
            # Obtener certificados iniciales (sin esperar a Nginx)
            docker-compose run --rm certbot certonly --webroot \
              --webroot-path /var/www/certbot \
              -d neureal.site -d www.neureal.site \
              --email ${{ secrets.CERTBOT_EMAIL }} \
              --agree-tos --non-interactive --force-renewal
            
            # Iniciar certbot en modo renovación
            docker-compose up -d certbot
          else
            echo "✅ SSL certificates already exist"
            # Solo iniciar certbot si no está corriendo
            docker-compose up -d certbot || true
          fi
          
          # Recargar Nginx para aplicar certificados
          docker-compose exec -T nginx nginx -s reload
          SSL_EOF

      - name: Verify & reload Nginx
        run: |
          ssh -o StrictHostKeyChecking=accept-new \
              ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }} << 'NGINX_EOF'
          set -e

          # Configuración de Nginx optimizada
          docker-compose exec -T nginx nginx -t
          echo "✅ Nginx configuration test passed"
          
          docker-compose exec -T nginx nginx -s reload
          echo "🔁 Nginx reloaded successfully"
          NGINX_EOF

      - name: Run health check
        run: |
          for i in {1..8}; do
            status=$(curl -s -o /dev/null -w "%{http_code}" https://neureal.site)
            if [ "$status" == "200" ]; then
              echo "✅ Health check passed with status 200"
              exit 0
            else
              echo "⚠️ Attempt $i: Health check failed (Status: $status). Retrying in 5s..."
              sleep 5
            fi
          done
          echo "❌ Health check failed after 8 attempts"
          exit 1

      - name: Performance test
        if: success()
        uses: treosh/lighthouse-ci-action@v11
        with:
          urls: |
            https://neureal.site
          uploadArtifacts: true
          temporaryPublicStorage: true

      - name: Notify Slack
        if: always()
        uses: rtCamp/action-slack-notify@v2
        env:
          SLACK_WEBHOOK: ${{ secrets.SLACK_WEBHOOK }}
          SLACK_CHANNEL: deployments
          SLACK_COLOR: ${{ job.status == 'success' && '#36a64f' || '#ff0000' }}
          SLACK_ICON_EMOJI: ${{ job.status == 'success' && ':rocket:' || ':fire:' }}
          SLACK_MESSAGE: "Deployment ${{ job.status }} for Neureal"
          SLACK_TITLE: "Production Deployment Report"
          SLACK_USERNAME: GitHub Actions Bot

      - name: Rollback on failure
        if: failure()
        run: |
          ssh -o StrictHostKeyChecking=no \
              ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }} << 'ROLLBACK_EOF'
          set -e
          echo "⚠️ Deployment failed! Starting rollback..."

          cd /opt/neureal
          git reset --hard HEAD@{1}

          # Detener servicios conflictivos
          sudo systemctl stop nginx apache2 || true
          
          docker-compose down --remove-orphans
          docker-compose up -d

          echo "🔙 Rollback completed successfully!"
          ROLLBACK_EOF